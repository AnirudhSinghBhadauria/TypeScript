const nomos: number[] = [4, 5, 6, 7, 8];

const superheros: string[] = ["thor", "spiderman", "ironman"];
// const superheros: Array<string> = ['thor', 'spiderman', 'ironman'];

type User = {
  name: string;
  isActive: boolean;
};

const newArray: User[] = [];

newArray.push({ name: "anirudh", isActive: true });

export {};
